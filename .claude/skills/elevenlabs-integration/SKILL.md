# ElevenLabs Integration
Use @elevenlabs/react SDK. WebRTC for <500ms latency. Signed URL pattern.
Backend generates signed URL (/api/conversation/signed-url). Client connects.
Real-time transcript via onMessage callback. Visual orb states.
